{
  "targets": {
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          {
            "command": "nx build frontend --watch"
          },
          {
            "command": "nx s electron"
          }
        ],
        "parallel": true
      }
    }
  }
}
