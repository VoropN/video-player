{
  "targets": {
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          {
            "command": "nx build frontend --watch --configuration development"
          },
          {
            "command": "nx s electron"
          }
        ],
        "parallel": true
      }
    }
  }
}
